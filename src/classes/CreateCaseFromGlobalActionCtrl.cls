public with sharing class CreateCaseFromGlobalActionCtrl {
    /*private Case csInstance {get; set;}

    public PageReference createCase() {
        try { 
            csInstance = new Case();
            insert csInstance;
            
            return new PageReference('/'+csInstance.Id);
    
        } catch(Exception e) {
            System.debug(LoggingLevel.Error, 'Erorr = '+e.getMessage());
            return null;
        }
        //PageReference pgRef = new PageReference('https://ldlt-qa-dev-ed.lightning.force.com/one/one.app?source=alohaHeader#/sObject/Case/new?count=1');
        //PageReference pgRef = new PageReference('/setup/ui/recordtypeselect.jsp?ent=Case');
        //&save_new_url=%2F500%2Fe%3F&retURL={!Opportunity.Id}'
        
        //return pgRef;
    }*/
}